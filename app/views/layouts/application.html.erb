<!DOCTYPE html>
<html>
  <head prefix="og: https://ogp.me/ns#">
    <title>Oshimotomu</title>
    <meta property="og:url" content=<%= request.url %> />
    <meta property="og:type" content="article" />
    <meta property="og:title" content=<%= content_for?(:og_title) ? yield(:og_title) : '' %> />
    <meta property="og:site_name" content=<%= t('defaults.application') %> />
    <meta property="og:image" content=<%= content_for?(:image_url) ? yield(:image_url) : 'https://oshimotomu.fly.dev/assets/images/ogp_default.png' %> name="ogpImage" />
    <meta property="og:discription" content='推しを見つける。布教する。' />
    <meta name="twitter:card" content="summary_large_image" />
    <!-- 「twitter:title」「twitter:description」「twitter:image」はogで設定しているためなくてもOK-->
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-KNEZZ6SX4B"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-KNEZZ6SX4B');
    </script>
    <!-- Google tag (gtag.js) -->
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render 'shared/header' %>
    <%= render 'shared/breadcrumbs' %>
    <%= render 'shared/flash_message' %>
    <%= yield %>
    <%= render 'shared/footer' %>
  </body>
</html>
